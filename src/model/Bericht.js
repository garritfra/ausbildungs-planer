export default class Bericht {
  constructor(id, activities, instructions, school) {
    this.id = id;
    this.activities = activities;
    this.instructions = instructions;
    this.school = school;
  }
}
